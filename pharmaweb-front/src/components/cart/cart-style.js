const cartItemsStyle = theme => ({
    root: {
      display: 'flex',
      flexWrap: 'wrap',
      justifyContent: 'center',
      overflow: 'hidden',
      height: '100%',
      width: '100%',
      backgroundColor: theme.palette.background.paper,
      marginTop: 32
    }
});

export default cartItemsStyle;
